---
import { ViewTransitions } from "astro:transitions";
import Navbar from "@/components/Navbar.astro";
import "@/assets/styles/main.css";

interface Props {
  title: string;
}

const { title } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <slot name="meta" />
    <title>{title}</title>
    <ViewTransitions />
    <link rel="preload" as="style" href="https://cdn.jsdelivr.net/npm/gardevoir" onload="this.rel='stylesheet';this.onload=null" />
    <noscript>
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/gardevoir" />
    </noscript>
    <script src="../utils/head.ts"></script>
  </head>
  <body>
    <header>
      <a id="skip-link" href="#content">Skip to content</a>
      <Navbar />
    </header>
    <main id="content" transition:animate="fade">
      <slot />
    </main>
  </body>
</html>

<style is:global>
  a#skip-link {
    position: absolute;
    left: 0.35rem;
    padding: 0.5rem;
    color: var(--background);
    background-color: var(--text);
    text-decoration-color: var(--background);
    transform: translateY(-100%);
    transition: transform 0.3s ease-in-out;

    &:hover {
      color: var(--text);
      background-color: var(--background);
      text-decoration-color: var(--text);
    }
    
    &:focus {
      transform: translateY(20%);
      box-shadow: none;
      outline: max(1px, 2px) dashed var(--text);
      outline-offset: 0.25em;
    }
  }

  main {
    display: grid;
    grid-template-columns: 1fr min(65ch, 100%) 1fr;
    justify-content: center;
    margin: 0 auto 5rem;
    padding: 0 1rem;

    & > * { grid-column: 2; }
    
    & > :not(h1, h2, h3, h4, h5, h6, button, input, textarea) {
      font-size: 1.125em;

      @supports (font-feature-settings: "calt") and (font-stretch: 110%) {
        font-feature-settings: "calt";
        font-stretch: 110%;
      }
    }
    p { line-height: 1.75; }
    & > ul:not(.tag) {
      list-style-type: square;
      margin: 0.5rem 1rem;
      padding: 0 1.5rem;

      & > li { padding: 0.25rem 0.5rem; }
    }
  }
</style>